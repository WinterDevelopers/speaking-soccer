// to select items you want
function categoryFunc(){
   var category = document.querySelectorAll('.ice');
   var cateoryText = category.textContent;
   var cat = [category]

    // filters out items that has a certian variable
    /*for (var i = 0; i < category.length; i++){
        if (2>1){
            category[3].className = "dont";
        }
    }
    console.log("hello")*/
      
}
